<%= link_to((image_tag("icons/cancel.png", :size => "16x16") + " " + t(:close)).html_safe, self.send("#{@object.class.to_s.underscore}_path", 0, {:ajax_id => @ajax_id, :close => true}), :remote => true, :class => "btn btn-default") %>
